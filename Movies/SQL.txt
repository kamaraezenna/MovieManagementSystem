
CREATE TABLE USERS (
    email VARCHAR(100) PRIMARY KEY,
    password VARCHAR(100) NOT NULL,
    name VARCHAR(100) NOT NULL
);

CREATE TABLE MOVIES (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    director VARCHAR(255) NOT NULL,
    year INTEGER NOT NULL,
    userEmail VARCHAR(100),
    FOREIGN KEY (userEmail) REFERENCES Users(email) ON DELETE CASCADE
);

INSERT INTO USERS (email, password, name)
VALUES 
('john.doe@example.com', '12345', 'John Doe'),
('jane.smith@example.com', 'abcde', 'Jane Smith');

INSERT INTO Movies (title, director, year, userEmail)
VALUES
('Inception', 'Christopher Nolan', 2010, 'john.doe@example.com'),
('The Matrix', 'Lilly Fairgrieve', 1999, 'john.doe@example.com'),
('Inside Out', 'Jessica Faan', 2014, 'jane.smith@example.com'),
('The Social Network', 'David Fincher', 2010, 'jane.smith@example.com');
